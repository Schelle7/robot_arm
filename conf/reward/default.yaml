pose_weights:
  position: 1.0
  rotation_primary: 1.0
  rotation_secondary: 0.5
  gripper: 1.0
  gripper_duty: 1.0
tracking:
  progress: true
joint_limit_penalty: true
termination_penalty: true
sustained_duty_penalty: true
joint_limit_penalty_factor: 1.0

# Thermal cost of holding duty. Anything up to the allowance is free, since the arm cannot hold
# itself up on nothing, and only the excess is charged, per joint.
sustained_duty_allowance: 1.0  # not active currently
sustained_duty_penalty_factor: 1.0
