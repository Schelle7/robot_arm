# @package _global_
# One low-level control step per high-level action for initial debugging.
device: "cuda"

control:
  frequencies:
    high_level: 5
    low_level: 5
  action_scale_radians_per_second: 0.05

waypoint:
  trajectory_length: 1

training:
  total_training_steps: 120000